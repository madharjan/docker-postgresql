#!/bin/sh

set -e
POSTGRESQL_VERSION=${POSTGRESQL_VERSION:-9.3}
POSTGRESQL_DATA_DIR=${POSTGRESQL_DATA_DIR:-/var/lib/postgresql/data}

exec /sbin/setuser postgres /usr/lib/postgresql/${POSTGRESQL_VERSION}/bin/postmaster -D ${POSTGRESQL_DATA_DIR} 2>&1
